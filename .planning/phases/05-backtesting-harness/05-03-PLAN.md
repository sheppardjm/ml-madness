---
phase: 05-backtesting-harness
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - src/backtest/validate.py
autonomous: true

must_haves:
  truths:
    - "2025 backtest uses only data available before 2025 Selection Sunday -- training set max season is 2024"
    - "2025 ESPN score is approximately 1200/1920 (model predicts all 4 #1 seeds to Final Four but misses championship)"
    - "Per-year Brier scores from backtest match the Phase 3 evaluation_results.json values to 4 decimal places (same best_C, same random_state=42, same walk-forward fold boundaries)"
    - "Re-running backtest() produces byte-identical backtest/results.json output (reproducibility)"
    - "All 4 Phase 5 success criteria pass in a single validation run"
  artifacts:
    - path: "src/backtest/validate.py"
      provides: "Phase 5 validation: temporal isolation check, 2025 verification, reproducibility check, success criteria summary"
      exports: ["validate_phase5"]
  key_links:
    - from: "src/backtest/validate.py"
      to: "src/backtest/backtest.py"
      via: "backtest() function call for validation"
      pattern: "from src.backtest.backtest import"
    - from: "src/backtest/validate.py"
      to: "backtest/results.json"
      via: "Read and compare backtest results"
      pattern: "backtest/results.json"
    - from: "src/backtest/validate.py"
      to: "models/evaluation_results.json"
      via: "Cross-reference Phase 3 per-year Brier scores for consistency"
      pattern: "evaluation_results.json"
---

<objective>
Validate the complete Phase 5 backtest harness against all 4 success criteria, with specific attention to temporal isolation (2025 uses only pre-2025 data), 2025 bracket scoring verification, and reproducibility.

Purpose: This validation plan confirms that the backtest results are trustworthy: no data leakage, metrics match Phase 3 evaluation, and results are reproducible. It also specifically satisfies BACK-01 (score 2025 tournament against in-repo results data) by verifying the 2025 ESPN score and per-round breakdown match the values discovered during research.

Output:
- `src/backtest/validate.py` -- Phase 5 validation module with `validate_phase5()` callable
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-backtesting-harness/05-RESEARCH.md
@.planning/phases/05-backtesting-harness/05-02-SUMMARY.md
@src/backtest/backtest.py
@src/backtest/scoring.py
@models/evaluation_results.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create validate.py with Phase 5 success criteria checks</name>
  <files>src/backtest/validate.py</files>
  <action>
Create `src/backtest/validate.py` with the following:

**Function `validate_phase5()`:**
Runs all Phase 5 success criteria checks sequentially. Prints a formatted summary.

**Criterion 1: Backtest produces per-year table**
- Load backtest/results.json (run backtest() first if it doesn't exist).
- Verify it has 4 entries in per_year (2022, 2023, 2024, 2025).
- Verify each entry has: brier, log_loss, accuracy, espn_score, per_round_accuracy, upset_detection_rate.
- Print PASS/FAIL.

**Criterion 2: Temporal isolation (2025 uses only pre-2025 data)**
- Re-fit model for 2025 and verify training data max season is 2024.
- Load full matchup dataset, filter to Season < 2025, and assert (not just print):
  `assert train_df['Season'].max() == 2024, f"Temporal leak: max train season is {train_df['Season'].max()}, expected 2024"`
  This MUST fail loudly if temporal isolation is broken.
- Cross-reference: load `models/evaluation_results.json` and compare per-year Brier scores. If the backtest recomputes Brier using the same best_C, random_state=42, and walk-forward fold (Season < test_year), the values should match to 4 decimal places (or very close). Print comparison table showing Phase 3 vs Phase 5 Brier per year.
- If they match (within 0.001 tolerance): PASS (confirms same temporal boundaries and same model).
- If they diverge: WARN with the delta (could indicate subtle differences in dataset construction).

**Criterion 3: Multi-year coverage (all 4 variance profiles)**
- Verify results contain entries for all 4 years: 2022, 2023, 2024, 2025.
- Print the upset profile per year (n_upsets from results).
- Verify each year has distinct n_upsets values (showing different variance profiles).
- Print PASS/FAIL.

**Criterion 4: Reproducibility**
- Read current backtest/results.json.
- Run backtest() again to produce a fresh result.
- Compare the two results dicts: per_year metrics should be identical (ignore generated_at timestamp).
- If identical: PASS. If different: FAIL with the first difference found.

**Print final summary:**
```
=== PHASE 5 SUCCESS CRITERIA CHECK ===
[PASS] 1. backtest() produces per-year table with all required metrics (4 years, ESPN + Brier + accuracy + upsets)
[PASS] 2. Temporal isolation: 2025 training max season = 2024; Brier matches Phase 3 within 0.001
[PASS] 3. Multi-year coverage: 2022 ({n} upsets), 2023 ({n} upsets), 2024 ({n} upsets), 2025 ({n} upsets)  # derive counts dynamically from per_year[i]['n_upsets']
[PASS] 4. Reproducibility: re-run produces identical results

Phase 5: 4/4 criteria PASS
```

Also print the 2025 bracket breakdown (BACK-01 validation):
```
=== 2025 BACKTEST VERIFICATION (BACK-01) ===
ESPN Score: 1200 / 1920 (62.5%)
Round of 64: 22/32 (68.8%)
Round of 32: 7/16 (43.8%)
Sweet 16: 5/8 (62.5%)
Elite 8: 4/4 (100.0%)
Final Four: 2/2 (100.0%)
Championship: 0/1 (0.0%)
```

**Add `__main__` block:**
```python
if __name__ == "__main__":
    validate_phase5()
```

Run: `uv run python -m src.backtest.validate`

IMPORTANT NOTES:
- The Brier comparison with evaluation_results.json may show small differences if the Phase 3 evaluation used a slightly different dataset or rounding. Use 0.001 tolerance and WARN (not FAIL) if outside tolerance.
- For reproducibility check, ignore the `generated_at` field since it contains the run date.
- The 2025 ESPN score of ~1200 and per-round breakdown are the key BACK-01 deliverable.
- validate_phase5() MUST assert the 2025 ESPN score falls within a valid range:
  `espn_2025 = [r for r in results['per_year'] if r['year'] == 2025][0]['espn_score']`
  `assert 1100 <= espn_2025 <= 1300, f"2025 ESPN score {espn_2025} outside expected range [1100, 1300]"`
  Research verified 1200 exactly; the range allows minor simulation variance.
  </action>
  <verify>
Run: `uv run python -c "
from src.backtest.validate import validate_phase5
validate_phase5()

# Additional range assertion for 2025 ESPN score
import json, pathlib
results = json.loads(pathlib.Path('backtest/results.json').read_text())
espn_2025 = [r for r in results['per_year'] if r['year'] == 2025][0]['espn_score']
assert 1100 <= espn_2025 <= 1300, f'2025 ESPN score {espn_2025} outside expected range [1100, 1300]'
print(f'2025 ESPN score {espn_2025} within expected range [1100, 1300] -- OK')
print('Phase 5 validation complete')
"
  </verify>
  <done>validate_phase5() runs all 4 Phase 5 success criteria checks. Temporal isolation is verified (2025 training max = 2024). Brier scores compared against Phase 3 evaluation_results.json. Multi-year coverage confirmed with distinct upset profiles. Reproducibility confirmed by re-run comparison. 2025 bracket breakdown printed (BACK-01). All criteria pass.</done>
</task>

</tasks>

<verification>
After task completes:
1. `uv run python -m src.backtest.validate` runs without unhandled errors
2. All 4 success criteria print PASS
3. Temporal isolation verified: 2025 training data contains only seasons < 2025
4. Brier scores match Phase 3 evaluation_results.json (within 0.001 tolerance)
5. All 4 backtest years present with distinct upset profiles
6. Re-running backtest() produces identical results (deterministic)
7. 2025 ESPN score and per-round breakdown printed for BACK-01 validation
</verification>

<success_criteria>
- validate_phase5() completes with 4/4 criteria PASS
- 2025 backtest temporal isolation confirmed (max training season = 2024)
- Reproducibility confirmed (re-run produces identical per-year metrics)
- 2025 ESPN score approximately 1200/1920 with per-round breakdown matching research values
- Phase 3 Brier consistency confirmed (within tolerance)
</success_criteria>

<output>
After completion, create `.planning/phases/05-backtesting-harness/05-03-SUMMARY.md`
</output>
