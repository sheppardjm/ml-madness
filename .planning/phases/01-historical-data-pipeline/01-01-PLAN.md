---
phase: 01-historical-data-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/__init__.py
  - src/ingest/__init__.py
  - src/ingest/kaggle_download.py
  - src/normalize/__init__.py
  - src/utils/__init__.py
  - src/utils/cutoff_dates.py
  - src/utils/seasons.py
autonomous: true
user_setup:
  - service: kaggle
    why: "Download NCAA tournament dataset"
    env_vars:
      - name: KAGGLE_USERNAME
        source: "https://www.kaggle.com/settings -> API section -> Create New Token -> downloads kaggle.json"
      - name: KAGGLE_KEY
        source: "Same kaggle.json file contains both username and key"
    dashboard_config:
      - task: "Accept competition rules"
        location: "https://www.kaggle.com/competitions/march-machine-learning-mania-2026/data -> click 'I Understand and Accept'"

must_haves:
  truths:
    - "Running `uv run python -c 'import duckdb; import pandas; import kagglehub'` succeeds without error"
    - "The data/raw/kaggle/ directory contains MTeams.csv, MNCAATourneyCompactResults.csv, MNCAATourneySeeds.csv, MSeasons.csv, and MRegularSeasonCompactResults.csv"
    - "Calling get_cutoff(2025) returns '2025-03-16' and get_cutoff(2020) raises ValueError"
    - "VALID_TOURNEY_SEASONS contains exactly 22 seasons (2003-2019 + 2021-2025, no 2020)"
  artifacts:
    - path: "pyproject.toml"
      provides: "Project metadata and dependencies"
      contains: "duckdb"
    - path: "src/ingest/kaggle_download.py"
      provides: "Kaggle dataset download function"
      contains: "kagglehub"
    - path: "src/utils/cutoff_dates.py"
      provides: "Selection Sunday dates and cutoff enforcement"
      exports: ["SELECTION_SUNDAY_DATES", "get_cutoff"]
    - path: "src/utils/seasons.py"
      provides: "Valid tournament season list"
      exports: ["VALID_TOURNEY_SEASONS"]
  key_links:
    - from: "src/ingest/kaggle_download.py"
      to: "data/raw/kaggle/"
      via: "kagglehub.competition_download + shutil.copytree"
      pattern: "kagglehub\\.competition_download"
---

<objective>
Set up the project environment with uv and Python 3.12, install all Phase 1 dependencies, download the Kaggle March Machine Learning Mania dataset, and create the Selection Sunday cutoff dates utility module.

Purpose: Establish the project foundation — dependencies, directory structure, raw data, and date constants — so that subsequent plans can immediately begin data transformation.
Output: A working Python project with uv, downloaded Kaggle CSV files in data/raw/kaggle/, and utility modules for cutoff dates and valid seasons.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-historical-data-pipeline/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project scaffolding with uv, pyproject.toml, and directory structure</name>
  <files>
    pyproject.toml
    src/__init__.py
    src/ingest/__init__.py
    src/normalize/__init__.py
    src/utils/__init__.py
    src/utils/cutoff_dates.py
    src/utils/seasons.py
  </files>
  <action>
    1. Initialize the project with uv if pyproject.toml does not already exist:
       ```
       uv init --python 3.12
       ```
       If pyproject.toml already exists, skip init and just ensure Python 3.12 is specified.

    2. Add all Phase 1 dependencies:
       ```
       uv add duckdb pandas pyarrow kagglehub thefuzz python-Levenshtein
       ```

    3. Create the directory structure:
       ```
       data/raw/kaggle/       # Will hold downloaded Kaggle CSVs
       data/processed/        # Will hold output Parquet files
       data/seeds/            # Will hold hand-curated alias CSV
       src/ingest/            # Ingestion scripts
       src/normalize/         # Team name normalization
       src/utils/             # Utility modules
       ```
       Create empty `__init__.py` files in src/, src/ingest/, src/normalize/, src/utils/.

    4. Create `src/utils/cutoff_dates.py` with:
       - `SELECTION_SUNDAY_DATES` dict mapping season (int) to date string (YYYY-MM-DD) for 2003-2025 (excluding 2020). Use the exact dates from the RESEARCH.md.
       - `get_cutoff(season: int) -> str` function that returns the date string or raises ValueError for invalid/missing seasons.
       - `DAYNUM_SELECTION_SUNDAY = 132` constant (DayNum for Selection Sunday in Kaggle data).

    5. Create `src/utils/seasons.py` with:
       - `VALID_TOURNEY_SEASONS: list[int]` = list of all valid tournament seasons 2003-2025 excluding 2020 (22 seasons total).
       - `DAYNUM_ROUND_MAP: dict[int, str]` mapping DayNum values to round names (134='First Four', 135='First Four', 136='Round of 64', 137='Round of 64', 138='Round of 32', 139='Round of 32', 143='Sweet Sixteen', 144='Sweet Sixteen', 145='Elite Eight', 146='Elite Eight', 152='Final Four', 154='Championship').

    6. Add `data/` to .gitignore (raw and processed data should not be committed). Also add `.kaggle/`, `__pycache__/`, `*.pyc`, `.venv/`.
  </action>
  <verify>
    - `uv run python -c "import duckdb; import pandas; import pyarrow; import kagglehub; import thefuzz; print('All imports OK')"` prints "All imports OK"
    - `ls data/raw/kaggle data/processed data/seeds src/ingest src/normalize src/utils` shows all directories exist
    - `uv run python -c "from src.utils.cutoff_dates import get_cutoff, SELECTION_SUNDAY_DATES; assert get_cutoff(2025) == '2025-03-16'; assert len(SELECTION_SUNDAY_DATES) == 22; print('Cutoff OK')"` prints "Cutoff OK"
    - `uv run python -c "from src.utils.seasons import VALID_TOURNEY_SEASONS; assert 2020 not in VALID_TOURNEY_SEASONS; assert len(VALID_TOURNEY_SEASONS) == 22; print('Seasons OK')"` prints "Seasons OK"
  </verify>
  <done>
    pyproject.toml exists with all Phase 1 dependencies, directory structure is created, cutoff_dates.py returns correct dates for all valid seasons and raises on 2020, seasons.py lists 22 valid tournament seasons.
  </done>
</task>

<task type="auto">
  <name>Task 2: Download Kaggle dataset and verify file inventory</name>
  <files>
    src/ingest/kaggle_download.py
  </files>
  <action>
    1. Create `src/ingest/kaggle_download.py` with a `download_kaggle_data()` function that:
       - Uses `kagglehub.competition_download('march-machine-learning-mania-2026')` to download the dataset
       - Copies downloaded files to `data/raw/kaggle/` using `shutil.copytree(path, 'data/raw/kaggle/', dirs_exist_ok=True)`
       - Prints the list of files downloaded
       - Returns the Path to the raw data directory

    2. Add a `verify_kaggle_files()` function that checks for the required files:
       - MTeams.csv
       - MSeasons.csv
       - MNCAATourneyCompactResults.csv
       - MNCAATourneySeeds.csv
       - MNCAATourneySlots.csv
       - MRegularSeasonCompactResults.csv
       Raises FileNotFoundError listing any missing files.

    3. Add a `if __name__ == "__main__"` block that calls download_kaggle_data() then verify_kaggle_files(), and prints:
       - Total file count
       - Size of each CSV file
       - Max season in MNCAATourneyCompactResults.csv (to verify 2025 data is present -- use duckdb.sql to check: `SELECT MAX(Season) FROM read_csv('data/raw/kaggle/MNCAATourneyCompactResults.csv')`)

    IMPORTANT: The user must have `~/.kaggle/kaggle.json` with their API token and must have accepted the competition rules at kaggle.com before this will work. If kagglehub fails with an auth error, print a clear message explaining what the user needs to do.

    4. Run the download script:
       ```
       uv run python -m src.ingest.kaggle_download
       ```

    5. After download, run a quick verification query:
       ```python
       import duckdb
       result = duckdb.sql("SELECT MIN(Season) as min_season, MAX(Season) as max_season, COUNT(*) as total_games FROM read_csv('data/raw/kaggle/MNCAATourneyCompactResults.csv')").df()
       print(result)
       ```
       Expected: min_season around 1985, max_season 2025, total_games 2000+.
  </action>
  <verify>
    - `ls data/raw/kaggle/MTeams.csv data/raw/kaggle/MNCAATourneyCompactResults.csv data/raw/kaggle/MNCAATourneySeeds.csv data/raw/kaggle/MSeasons.csv` all exist
    - `uv run python -c "import duckdb; r = duckdb.sql(\"SELECT MAX(Season) as ms FROM read_csv('data/raw/kaggle/MNCAATourneyCompactResults.csv')\").fetchone(); print(f'Max season: {r[0]}'); assert r[0] >= 2024"` prints max season >= 2024
    - `uv run python -c "from src.ingest.kaggle_download import verify_kaggle_files; verify_kaggle_files(); print('All required files present')"` prints success
  </verify>
  <done>
    All required Kaggle CSV files are downloaded to data/raw/kaggle/, the verify function confirms all files present, and a DuckDB query confirms tournament data covers at least through 2024 (ideally 2025).
  </done>
</task>

</tasks>

<verification>
1. `uv run python -c "import duckdb, pandas, pyarrow, kagglehub, thefuzz"` -- all imports succeed
2. `data/raw/kaggle/` contains at minimum: MTeams.csv, MSeasons.csv, MNCAATourneyCompactResults.csv, MNCAATourneySeeds.csv, MRegularSeasonCompactResults.csv
3. `from src.utils.cutoff_dates import get_cutoff` works and returns correct dates
4. `from src.utils.seasons import VALID_TOURNEY_SEASONS` works and has 22 seasons (no 2020)
</verification>

<success_criteria>
- Python 3.12 project initialized with uv and all Phase 1 dependencies installed
- Kaggle dataset downloaded and all required CSV files verified present
- Selection Sunday cutoff dates module returns correct dates for 2003-2025 (minus 2020)
- Valid tournament seasons list excludes 2020 and contains exactly 22 entries
- Directory structure matches the research-recommended layout
</success_criteria>

<output>
After completion, create `.planning/phases/01-historical-data-pipeline/01-01-SUMMARY.md`
</output>
