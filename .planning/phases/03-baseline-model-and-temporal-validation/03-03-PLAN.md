---
phase: 03-baseline-model-and-temporal-validation
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/models/evaluate.py
  - models/calibration_curve.png
  - models/evaluation_results.json
autonomous: true

must_haves:
  truths:
    - "Brier score and log-loss are computed and printed for each holdout year (2022, 2023, 2024, 2025) in a formatted comparison table"
    - "Hard-chalk Brier score (higher seed always wins with P=1.0) is computed per year and shown alongside model Brier for direct comparison"
    - "The model's mean Brier score across all 4 holdout years is below 0.23"
    - "A calibration curve PNG shows predicted vs. actual win rates across 10 probability bins — the curve is close to the diagonal"
    - "No matchup between two top-10-seeded teams produces a win probability above 90% — the model does not produce overconfident predictions"
    - "Evaluation results are written to models/evaluation_results.json for programmatic downstream consumption"
  artifacts:
    - path: "src/models/evaluate.py"
      provides: "Brier score, log-loss, calibration evaluation pipeline"
      exports: ["evaluate_all_holdout_years", "compute_chalk_brier", "check_calibration"]
    - path: "models/calibration_curve.png"
      provides: "Reliability diagram for logistic baseline"
    - path: "models/evaluation_results.json"
      provides: "Per-year Brier, log-loss, chalk comparison in JSON format"
  key_links:
    - from: "src/models/evaluate.py"
      to: "src/models/train_logistic.py"
      via: "load_model() to get trained model"
      pattern: "from src.models.train_logistic import"
    - from: "src/models/evaluate.py"
      to: "src/models/temporal_cv.py"
      via: "walk_forward_splits() for holdout folds"
      pattern: "from src.models.temporal_cv import"
    - from: "src/models/evaluate.py"
      to: "src/models/features.py"
      via: "build_matchup_dataset() and FEATURE_COLS"
      pattern: "from src.models.features import"
---

<objective>
Build the evaluation pipeline that computes Brier score and log-loss per holdout year, compares against chalk baseline, generates a calibration curve, and validates that the model does not produce overconfident predictions.

Purpose: This plan answers the critical question: "Does the baseline model actually beat naive seed-based predictions?" It establishes the quantitative benchmark (Brier < 0.23) that the Phase 6 ensemble must beat, and the calibration check ensures the probabilities are trustworthy for downstream Monte Carlo simulation (Phase 4). The evaluation results become the first row in the Phase 7 model comparison dashboard.

Output:
- `src/models/evaluate.py` — evaluation pipeline with chalk comparison and calibration check
- `models/calibration_curve.png` — reliability diagram
- `models/evaluation_results.json` — machine-readable per-year metrics
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-baseline-model-and-temporal-validation/03-RESEARCH.md
@.planning/phases/03-baseline-model-and-temporal-validation/03-01-SUMMARY.md
@.planning/phases/03-baseline-model-and-temporal-validation/03-02-SUMMARY.md
@src/models/features.py
@src/models/temporal_cv.py
@src/models/train_logistic.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Brier score and log-loss evaluation with chalk comparison</name>
  <files>src/models/evaluate.py, models/evaluation_results.json</files>
  <action>
Create `src/models/evaluate.py` with:

**Imports:** sklearn.metrics (brier_score_loss, log_loss), sklearn.preprocessing.StandardScaler, sklearn.linear_model.LogisticRegression, numpy, pandas, json, pathlib

**Function `compute_chalk_brier(y_true)`:**
- Computes hard-chalk Brier score: the Brier score if you always predict P=1.0 that the higher seed (team_a) wins.
- Since label=1 means the higher seed won: `chalk_prob = np.ones(len(y_true))`.
- Return `brier_score_loss(y_true, chalk_prob)`.

**Function `evaluate_all_holdout_years(model_path='models/logistic_baseline.joblib', processed_dir='data/processed')`:**
- Imports `build_matchup_dataset`, `FEATURE_COLS` from `src.models.features`.
- Imports `walk_forward_splits`, `BACKTEST_YEARS` from `src.models.temporal_cv`.
- Imports `load_model` from `src.models.train_logistic`.
- Loads the trained model artifact (model, scaler object from training) — but NOTE: for honest evaluation, we must NOT use the scaler fit on all data. Instead, for each fold:
  1. Fit a NEW StandardScaler on the training fold only.
  2. Transform both train and test with this fold-specific scaler.
  3. Fit a NEW LogisticRegression with the same C parameter (from artifact['best_C']) on the training fold.
  4. Predict probabilities on the test fold.
- This is the correct walk-forward evaluation: model is re-fit per fold using only past data.
- For each holdout year, compute:
  - `brier = brier_score_loss(y_test, y_prob)`
  - `ll = log_loss(y_test, y_prob)`
  - `chalk_brier = compute_chalk_brier(y_test)`
  - `accuracy = (y_pred_class == y_test).mean()` where `y_pred_class = (y_prob >= 0.5).astype(int)`
  - `n_test = len(y_test)`
  - `n_upsets = (y_test == 0).sum()` (games where higher seed lost)
  - `upset_correct = ((y_prob < 0.5) & (y_test == 0)).sum()` (upsets the model predicted)
- Collect all per-year results into a list of dicts.
- Also collect all y_true and y_prob across folds (for calibration in Task 2).

**Print a formatted comparison table:**
```
=== BASELINE MODEL EVALUATION (Walk-Forward) ===

Year | Games | Brier  | Chalk  | Delta  | LogLoss | Accuracy | Upsets | Upset Hit
-----|-------|--------|--------|--------|---------|----------|--------|----------
2022 |    63 | 0.XXXX | 0.XXXX | +0.XXX | 0.XXXX  |   XX.X%  |   XX   |    XX
...
-----|-------|--------|--------|--------|---------|----------|--------|----------
Mean |   252 | 0.XXXX | 0.XXXX | +0.XXX | 0.XXXX  |   XX.X%  |   XX   |    XX

Threshold: Brier < 0.23 — PASS/FAIL
Model beats chalk every year: YES/NO
```

- Compute and print mean Brier across all years.
- Assert/print whether mean Brier < 0.23 (PASS/FAIL — do NOT assert, just print, since we want the evaluation to complete even if the model doesn't meet the threshold).

**Write results to JSON:**
```python
results = {
    'model': 'logistic_baseline',
    'best_C': artifact['best_C'],
    'per_year': per_year_results_list,
    'mean_brier': mean_brier,
    'mean_log_loss': mean_log_loss,
    'mean_accuracy': mean_accuracy,
    'beats_chalk_every_year': all(r['brier'] < r['chalk_brier'] for r in per_year_results),
    'below_023_threshold': mean_brier < 0.23,
}
pathlib.Path('models/evaluation_results.json').write_text(json.dumps(results, indent=2))
```

- Return the results dict and the concatenated (y_true_all, y_prob_all) arrays for calibration.
  </action>
  <verify>
Run: `uv run python -c "
import json, pathlib
assert pathlib.Path('models/evaluation_results.json').exists(), 'Results file missing'
results = json.loads(pathlib.Path('models/evaluation_results.json').read_text())
assert len(results['per_year']) == 4, f'Expected 4 years, got {len(results[\"per_year\"])}'
assert results['mean_brier'] < 0.30, f'Brier too high: {results[\"mean_brier\"]}'
for yr in results['per_year']:
    assert yr['brier'] < yr['chalk_brier'], f'Model worse than chalk in {yr[\"year\"]}'
    print(f'{yr[\"year\"]}: Brier={yr[\"brier\"]:.4f} Chalk={yr[\"chalk_brier\"]:.4f} Acc={yr.get(\"accuracy\", 0):.1%}')
print(f'Mean Brier: {results[\"mean_brier\"]:.4f} (threshold: < 0.23)')
print(f'Below 0.23: {results[\"below_023_threshold\"]}')
print('OK')
"`
  </verify>
  <done>evaluation_results.json exists with per-year Brier, log-loss, chalk comparison, accuracy, and upset detection for all 4 holdout years. The model beats hard-chalk Brier in every year. The formatted table is printed to stdout during evaluation.</done>
</task>

<task type="auto">
  <name>Task 2: Calibration curve and overconfidence check</name>
  <files>src/models/evaluate.py (append), models/calibration_curve.png</files>
  <action>
Add to `src/models/evaluate.py`:

**Function `check_calibration(y_true_all, y_prob_all, save_path='models/calibration_curve.png')`:**
- Uses `sklearn.calibration.CalibrationDisplay.from_predictions()` to create a reliability diagram.
- Parameters: `n_bins=10, strategy='uniform', name='Logistic Baseline'`.
- Add a reference diagonal line (already included by CalibrationDisplay).
- Title: "Calibration Curve - Logistic Baseline (2022-2025 Holdout)"
- Save to `save_path` with `dpi=150, bbox_inches='tight'`.
- Print bin-level summary: for each of the 10 bins, print the mean predicted probability and the actual fraction of positives. Use `sklearn.calibration.calibration_curve()` for this.
- Print max deviation from perfect calibration (max |predicted - actual| across bins).

**Function `check_top_seed_overconfidence(df, artifact, processed_dir='data/processed')`:**
- From the matchup dataset, filter to games where BOTH team_a_seed <= 10 AND team_b_seed <= 10 (top-10 seeded matchups — these are late-round games between strong teams).
- For each such game, compute predicted probability using the model.
- Assert that no predicted probability exceeds 0.90: "OVERCONFIDENCE: P={prob:.3f} for {team_a_seed} vs {team_b_seed} in {season}"
- If assertion would fail, print a WARNING instead (don't crash — just flag it).
- Print summary: N top-seed matchups checked, max probability observed, min probability observed.
- Return True if all probabilities are < 0.90, False otherwise.

**Update the `__main__` block** to:
1. Call `evaluate_all_holdout_years()` — prints the comparison table and writes JSON.
2. Call `check_calibration(y_true_all, y_prob_all)` — saves calibration curve PNG.
3. Call `check_top_seed_overconfidence()` — prints overconfidence check results.
4. Print final summary:
   ```
   === PHASE 3 SUCCESS CRITERIA CHECK ===
   [PASS/FAIL] 1. Model file exists at models/logistic_baseline.joblib
   [PASS/FAIL] 2. Walk-forward CV: 4 distinct non-overlapping holdout years
   [PASS/FAIL] 3. Mean Brier score < 0.23
   [PASS/FAIL] 4. No top-seed matchup has > 90% win probability
   ```
5. Run: `uv run python -m src.models.evaluate`
  </action>
  <verify>
Run: `uv run python -c "
import pathlib
assert pathlib.Path('models/calibration_curve.png').exists(), 'Calibration curve missing'
print(f'Calibration curve: {pathlib.Path(\"models/calibration_curve.png\").stat().st_size} bytes')
import json
results = json.loads(pathlib.Path('models/evaluation_results.json').read_text())
print(f'Mean Brier: {results[\"mean_brier\"]:.4f}')
print(f'Below 0.23: {results[\"below_023_threshold\"]}')
print('OK')
"`
  </verify>
  <done>models/calibration_curve.png exists showing the reliability diagram. The calibration curve is reasonably close to the diagonal (max deviation < 0.15 from perfect calibration). No top-seed matchup produces > 90% win probability. The Phase 3 success criteria summary is printed.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `models/evaluation_results.json` exists with per-year metrics for 2022, 2023, 2024, 2025
2. `models/calibration_curve.png` exists as a readable PNG
3. The model beats hard-chalk Brier in every holdout year
4. Mean Brier score is below 0.23 (or close to it with documented explanation)
5. No overconfident predictions for top-seed matchups
6. The Phase 3 success criteria checklist is printed with PASS/FAIL for each criterion
</verification>

<success_criteria>
- Brier score and log-loss printed for each of the 4 holdout years in a formatted table
- Model beats hard-chalk baseline in every holdout year (positive delta)
- Mean Brier score across 2022-2025 is below 0.23
- Calibration curve PNG saved showing predicted vs. actual win rates
- No top-10-seeded matchup produces > 90% win probability
- evaluation_results.json written with all metrics for downstream consumption (Phase 5, 7)
</success_criteria>

<output>
After completion, create `.planning/phases/03-baseline-model-and-temporal-validation/03-03-SUMMARY.md`
</output>
