---
phase: 09-bracket-visualization-ui
plan: 03
type: execute
wave: 2
depends_on: ["09-01", "09-02"]
files_modified:
  - src/ui/bracket_svg.py
  - app.py
autonomous: true

must_haves:
  truths:
    - "The Bracket tab displays a complete SVG bracket with all 67 game slots filled"
    - "Each game box shows both competing team names with seed numbers"
    - "Each game box shows the win probability for the predicted winner"
    - "Regions W and X are on the left half, Y and Z on the right half, Final Four in the center"
    - "The predicted champion is visually highlighted (distinct color or border)"
    - "Connector lines connect child slots to parent slots showing bracket progression"
  artifacts:
    - path: "src/ui/bracket_svg.py"
      provides: "SVG string builder: game boxes, connector lines, champion highlight"
      exports: ["render_bracket_svg_string"]
    - path: "app.py"
      provides: "Updated Bracket tab rendering SVG via st.components.v1.html"
      contains: "components.html"
  key_links:
    - from: "src/ui/bracket_svg.py"
      to: "src/ui/bracket_layout.py"
      via: "compute_bracket_layout() for slot coordinates"
      pattern: "compute_bracket_layout"
    - from: "src/ui/bracket_svg.py"
      to: "simulate_bracket output"
      via: "det_result['slots'] for team_id and win_prob per slot"
      pattern: "slots.*team_id"
    - from: "app.py"
      to: "src/ui/bracket_svg.py"
      via: "render_bracket_svg_string() called in tab_bracket"
      pattern: "render_bracket_svg_string"
---

<objective>
Build the SVG bracket rendering module and wire it into the Bracket tab of app.py.

Purpose: This plan transforms the coordinate layout (Plan 09-02) and simulation results (Plan 09-01) into a complete programmatic SVG string that displays the 68-team bracket with team names, seed numbers, win probabilities, and connector lines. The SVG is rendered in Streamlit via st.components.v1.html() with explicit height to avoid the 150px clipping pitfall.

Output: A working Bracket tab that displays the full tournament bracket as an interactive SVG with all 67 games filled, probabilities shown, and champion highlighted.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-bracket-visualization-ui/09-RESEARCH.md
@.planning/phases/09-bracket-visualization-ui/09-01-SUMMARY.md
@.planning/phases/09-bracket-visualization-ui/09-02-SUMMARY.md

@src/ui/data_loader.py
@src/ui/bracket_layout.py
@app.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/ui/bracket_svg.py SVG rendering module</name>
  <files>src/ui/bracket_svg.py</files>
  <action>
Create `src/ui/bracket_svg.py` that builds a complete SVG string from layout coordinates and simulation results.

**Function: `render_bracket_svg_string(det_result, layout, team_id_to_name, team_id_to_seednum, season=2025) -> str`**

Parameters:
- `det_result`: Dict from `simulate_bracket(mode="deterministic")` -- has `slots` dict and `champion` dict
- `layout`: Dict from `compute_bracket_layout()` -- has `slots`, `connectors`, `canvas_width`, `canvas_height`
- `team_id_to_name`: Dict mapping team_id -> display name string
- `team_id_to_seednum`: Dict mapping team_id -> integer seed number
- `season`: Tournament season year

Returns: Complete SVG string (not HTML -- the caller wraps in HTML).

**SVG structure:**

1. SVG root element with `viewBox="0 0 {canvas_width} {canvas_height}"` and explicit width/height attributes. Use a dark background color (#0e1117) to match Streamlit dark theme.

2. For each slot in layout["slots"]:
   a. Get the simulation result from `det_result["slots"][slot_id]` -- contains `team_id`, `win_prob`, `round`
   b. Look up the two teams competing in this slot:
      - For the WINNER: use `det_result["slots"][slot_id]["team_id"]` and look up name/seed
      - For the slot's two INPUT teams: use the slot_tree structure. The StrongSeed and WeakSeed of the slot reference either a seed label (look up in seedings) or a prior slot winner (look up in det_result). Build a helper to resolve these.
   c. Draw a game box at (x, y, w, h) from the layout:
      - Background rectangle with rounded corners, fill color based on whether this is the champion slot (#1e5a3a for champion, #1a1a2e for normal), stroke #4a90d9
      - Two rows inside the box:
        - Top row: team A (StrongSeed's resolved team) -- show "(seed) TeamName  prob%" with the winner's text bold/bright and the loser's text dimmed
        - Bottom row: team B (WeakSeed's resolved team) -- same format
      - The winner row gets highlighted text (#ffffff) and the loser row gets dimmed (#888888)
      - Win probability shown on the winner's row, right-aligned, in green (#4ec9b0)
   d. For right-side regions (Y, Z -- where `side == "right"`): text is still left-aligned within the box (SVG text is always left-to-right), but the box x-position is already mirrored by the layout module.

3. For each connector in layout["connectors"]:
   - Draw a polyline or path connecting the points with stroke #4a90d9, stroke-width 1, fill none
   - Use L-shaped connectors: horizontal from child output -> vertical -> horizontal into parent input

4. Region labels: Add text labels for each region name ("WEST", "EAST", "SOUTH", "MIDWEST" -- mapped from W, X, Y, Z) positioned above each region's R1 column.

5. Center column labels: "FINAL FOUR" above R5 slots, "CHAMPIONSHIP" above R6CH.

**Helper function: `_resolve_slot_teams(slot_id, slot_tree, seedings, det_result) -> tuple[int, int]`**
Returns (strong_team_id, weak_team_id) for a given slot by resolving the StrongSeed and WeakSeed references through the slot tree and simulation results.

**Helper function: `_svg_game_box(x, y, w, h, strong_name, strong_seed, weak_name, weak_seed, winner_id, strong_id, weak_id, win_prob, is_champion, side) -> str`**
Returns an SVG `<g>` group with the game box rectangle and text elements.

**Font:** Use `font-family="Consolas, Monaco, monospace"` for consistent character widths. Font size 9px for team names, 8px for seed labels and probabilities. Truncate team names longer than 14 characters with "..." to prevent overflow.

**Color scheme (dark theme):**
- Background: #0e1117
- Normal box fill: #1a1a2e
- Champion box fill: #1e5a3a (green tint)
- Box stroke: #4a90d9
- Winner text: #ffffff
- Loser text: #666666
- Probability text: #4ec9b0
- Connector lines: #3a6a9a
- Region labels: #888888
  </action>
  <verify>
`uv run python -c "
from src.ui.bracket_svg import render_bracket_svg_string
from src.ui.bracket_layout import compute_bracket_layout
from src.simulator.bracket_schema import load_seedings, build_predict_fn, build_slot_tree
from src.simulator.simulate import simulate_bracket
import duckdb

seedings = load_seedings(2025)
predict_fn, stats = build_predict_fn(season=2025)
det_result = simulate_bracket(seedings, predict_fn, mode='deterministic', season=2025)
layout = compute_bracket_layout(2025)

conn = duckdb.connect()
df = conn.execute(\"SELECT TeamID, TeamName, SeedNum FROM read_parquet('data/processed/seeds.parquet') WHERE Season=2025\").df()
conn.close()
team_id_to_name = dict(zip(df.TeamID, df.TeamName))
team_id_to_seednum = dict(zip(df.TeamID, df.SeedNum))

svg = render_bracket_svg_string(det_result, layout, team_id_to_name, team_id_to_seednum)
assert '<svg' in svg
assert '</svg>' in svg
assert 'R6CH' in svg or 'CHAMPIONSHIP' in svg
print(f'SVG length: {len(svg)} chars')
print('SVG render OK')
"` -- should print SVG length and 'SVG render OK'.
  </verify>
  <done>
1. src/ui/bracket_svg.py exists with render_bracket_svg_string() function
2. SVG string contains game boxes for all 67 slots
3. Each game box shows two team names with seeds and the winner's probability
4. Champion slot has distinct visual styling
5. Connector lines drawn between parent-child slots
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire SVG bracket and champion panel into app.py tabs</name>
  <files>app.py</files>
  <action>
Update `app.py` to replace the placeholder content in the Bracket tab and Champion tab with actual rendered content.

**Bracket tab changes:**
1. Import `render_bracket_svg_string` from `src.ui.bracket_svg`
2. Import `compute_bracket_layout` from `src.ui.bracket_layout`
3. Import `build_slot_tree` from `src.simulator.bracket_schema` (needed by SVG renderer to resolve slot teams)
4. Import `streamlit.components.v1 as components`
5. In the `with tab_bracket:` block:
   - Call `layout = compute_bracket_layout(season)` (can be cached with @st.cache_data at module level or inline)
   - Call `svg_string = render_bracket_svg_string(det_result, layout, team_id_to_name, team_id_to_seednum, season)`
   - Wrap the SVG in minimal HTML: `<html><body style="margin:0; background:#0e1117; overflow:auto;">{svg_string}</body></html>`
   - Render with `components.html(html_string, height=layout["canvas_height"] + 40, scrolling=True)`
   - The +40 accounts for padding so the bottom of the bracket isn't clipped
   - CRITICAL: Pass explicit `height=` to prevent the 150px default clipping (Research pitfall 1)

**Champion tab changes:**
Replace the placeholder with a champion panel:
1. Display the deterministic champion prominently:
   - `st.header(f"Predicted Champion: {champ_name}")` where champ_name comes from the sidebar logic already computed
   - `st.metric("Championship Win Probability", f"{champion['win_prob']:.1%}")`
2. Display Monte Carlo confidence:
   - `st.metric("Monte Carlo Confidence", f"{mc_champ['confidence']:.1%}", help="Fraction of 10,000 simulations where this team wins the tournament")`
3. Display championship game score if available:
   - If `det_result.get("championship_game")` is not None, display predicted score
   - Otherwise show `st.caption("Championship game score requires stats_lookup (not provided in this session)")
4. Display top 10 contenders from MC results:
   - Build a DataFrame from mc_result["advancement_probs"] showing teams with highest "Champion" probability
   - Sort by Champion probability descending
   - Show as st.dataframe with columns: Team, Seed, Champion %

**Do NOT modify the Advancement tab** -- that is Plan 09-04's responsibility. Leave the placeholder intact.
  </action>
  <verify>
1. `uv run python -c "import ast; ast.parse(open('app.py').read()); print('Syntax OK')"` -- no syntax errors
2. Start Streamlit briefly: `timeout 15 uv run streamlit run app.py --server.headless true` -- process should start without immediate crash
3. Check that app.py contains `components.html` (bracket rendering) and `st.metric` (champion panel)
  </verify>
  <done>
1. Bracket tab renders SVG bracket via st.components.v1.html with explicit height
2. Champion tab shows predicted champion name, win probability, MC confidence, and top 10 contenders
3. Advancement tab placeholder remains for Plan 09-04
4. App launches without errors
  </done>
</task>

</tasks>

<verification>
1. `uv run python -c "from src.ui.bracket_svg import render_bracket_svg_string; print('SVG module OK')"` -- import succeeds
2. `uv run python -c "import ast; ast.parse(open('app.py').read()); print('Syntax OK')"` -- app.py is valid Python
3. `uv run streamlit run app.py --server.headless true` starts without crash (bracket SVG renders, champion panel shows data)
4. The SVG contains team names (not just numeric IDs) and probability values
</verification>

<success_criteria>
- Bracket tab displays a complete 68-team bracket SVG with all 67 games
- Each game slot shows team names, seed numbers, and win probability
- Four regions visible with left/right layout and center Final Four
- Champion slot visually highlighted
- Champion tab shows champion name, probability, MC confidence, and top contenders
- No 150px height clipping (explicit height passed to components.html)
</success_criteria>

<output>
After completion, create `.planning/phases/09-bracket-visualization-ui/09-03-SUMMARY.md`
</output>
