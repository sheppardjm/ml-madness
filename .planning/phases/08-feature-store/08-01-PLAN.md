---
phase: 08-feature-store
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/models/features.py
  - src/simulator/bracket_schema.py
  - src/backtest/backtest.py
autonomous: true

must_haves:
  truths:
    - "compute_features(team_a='Duke', team_b='Michigan', season=2025) returns a dict with all 6 FEATURE_COLS keys"
    - "Existing backtest and simulator code still works after the rename (no regressions)"
    - "Unknown team name raises ValueError with helpful message"
    - "Name resolution covers canonical_name, kaggle_name, and cbbdata_name from team_normalization.parquet"
  artifacts:
    - path: "src/models/features.py"
      provides: "Public name-based compute_features() API + internal _compute_features_by_id()"
      contains: "def compute_features(team_a: str"
    - path: "pyproject.toml"
      provides: "pytest and statsmodels dependencies"
      contains: "pytest"
  key_links:
    - from: "src/models/features.py"
      to: "data/processed/team_normalization.parquet"
      via: "_resolve_team_id() reads canonical_name, kaggle_name, cbbdata_name"
      pattern: "_resolve_team_id"
    - from: "src/backtest/backtest.py"
      to: "src/models/features.py"
      via: "imports _compute_features_by_id (renamed from compute_features)"
      pattern: "_compute_features_by_id"
    - from: "src/simulator/bracket_schema.py"
      to: "src/models/features.py"
      via: "imports _compute_features_by_id (renamed from compute_features)"
      pattern: "_compute_features_by_id"
---

<objective>
Add pytest and statsmodels dependencies, create the public name-based `compute_features()` API in features.py, and update all existing call sites to use the renamed internal function `_compute_features_by_id()`.

Purpose: SC-1 requires `compute_features(team_a="Duke", team_b="Michigan", season=2025)` -- a name-based API. The current function takes integer IDs. This plan creates the public wrapper with name-to-ID resolution and renames the internal function to avoid signature collision, while keeping all existing callers working.

Output: Updated `features.py` with both APIs, updated call sites in `backtest.py` and `bracket_schema.py`, new dependencies in `pyproject.toml`.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-feature-store/08-RESEARCH.md

@src/models/features.py
@src/backtest/backtest.py
@src/simulator/bracket_schema.py
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dependencies and pytest config to pyproject.toml</name>
  <files>pyproject.toml</files>
  <action>
    Run `uv add pytest>=9.0.2 statsmodels>=0.14.6` to add both dependencies.

    Then add pytest configuration to pyproject.toml:
    ```toml
    [tool.pytest.ini_options]
    testpaths = ["tests"]
    pythonpath = ["."]
    addopts = "-v"
    ```

    After adding, run `uv sync` to install.
  </action>
  <verify>
    `uv run python -c "import pytest; print(pytest.__version__)"` prints version >= 9.0.2
    `uv run python -c "import statsmodels; print(statsmodels.__version__)"` prints version >= 0.14.6
    `grep -A2 'tool.pytest' pyproject.toml` shows the ini_options section
  </verify>
  <done>Both pytest and statsmodels are installed and importable; pyproject.toml has [tool.pytest.ini_options] with pythonpath=["."]</done>
</task>

<task type="auto">
  <name>Task 2: Create name-based compute_features() API and update all call sites</name>
  <files>src/models/features.py, src/simulator/bracket_schema.py, src/backtest/backtest.py</files>
  <action>
    In `src/models/features.py`:

    1. Rename the existing `compute_features()` function to `_compute_features_by_id()`. Keep its exact signature: `_compute_features_by_id(season, team_a_id, team_b_id, stats_lookup)`. Update the docstring to note it is the internal ID-based function.

    2. Add a module-level `_TEAM_NAME_LOOKUP: dict[str, int] | None = None` cache.

    3. Add `_get_name_lookup(processed_dir)` function that lazily builds a dict mapping team names (canonical_name, kaggle_name, cbbdata_name) to kaggle_team_id from `team_normalization.parquet`. Uses DuckDB to read the parquet. Caches in `_TEAM_NAME_LOOKUP`. Skip empty strings (espn_name has many empty strings per decision [02-02]).

    4. Add `_resolve_team_id(name, processed_dir)` function that calls `_get_name_lookup()` and returns the kaggle_team_id. Raises `ValueError` with a helpful message if name not found. Include 3-4 example team names in the error message.

    5. Add the NEW public `compute_features()` function with this exact signature:
    ```python
    def compute_features(
        team_a: str,
        team_b: str,
        season: int,
        stats_lookup: dict[tuple[int, int], dict[str, Any]] | None = None,
        processed_dir: str | pathlib.Path = "data/processed",
    ) -> dict[str, float]:
    ```
    This function:
    - Resolves team_a and team_b to IDs via `_resolve_team_id()`
    - If stats_lookup is None, calls `build_stats_lookup(processed_dir)` to build it
    - Delegates to `_compute_features_by_id(season, team_a_id, team_b_id, stats_lookup)`
    - Returns the same dict as before (keys = FEATURE_COLS)

    6. Update the module docstring exports list to show the new public API and note `_compute_features_by_id()` as internal.

    7. Update `build_matchup_dataset()` internal call from `compute_features()` to `_compute_features_by_id()` (line ~305 in the current file).

    In `src/simulator/bracket_schema.py`:
    - Change import from `compute_features` to `_compute_features_by_id` (line ~290)
    - Update the call on line ~311 from `compute_features(season, ...)` to `_compute_features_by_id(season, ...)`

    In `src/backtest/backtest.py`:
    - Change import from `compute_features` to `_compute_features_by_id` (line ~65)
    - Update both call sites (~line 433 and ~line 481) from `compute_features(` to `_compute_features_by_id(`

    IMPORTANT: Do NOT change the internal function's behavior or signature. Only the name changes from `compute_features` to `_compute_features_by_id`. All 4 positional args (season, team_a_id, team_b_id, stats_lookup) remain the same.
  </action>
  <verify>
    1. `uv run python -c "from src.models.features import compute_features; feats = compute_features('Duke', 'Michigan', 2025); print(feats)"` -- should print a dict with all 6 feature keys and numeric values
    2. `uv run python -c "from src.models.features import compute_features; compute_features('Totally Fake University', 'Duke', 2025)"` -- should raise ValueError with "not found in team_normalization"
    3. `uv run python -c "from src.models.features import _compute_features_by_id, build_stats_lookup; sl = build_stats_lookup(); print(_compute_features_by_id(2025, 1181, 1276, sl))"` -- should print same dict as step 1 (Duke=1181, Michigan=1276)
    4. `uv run python -c "from src.models.features import build_matchup_dataset; df = build_matchup_dataset(); print(df.shape)"` -- should print (1054, 12) or similar, no errors
    5. `uv run python -m src.simulator.bracket_schema 2>/dev/null; echo $?` -- exit code 0 (no import errors)
  </verify>
  <done>
    - `compute_features("Duke", "Michigan", 2025)` returns a dict with adjoe_diff, adjde_diff, barthag_diff, seed_diff, adjt_diff, wab_diff
    - Unknown team name raises ValueError
    - `_compute_features_by_id()` still works with integer IDs for internal callers
    - `build_matchup_dataset()`, backtest, and simulator all import and run without errors
  </done>
</task>

</tasks>

<verification>
1. Name-based API works: `compute_features("Duke", "Michigan", 2025)` returns 6-key dict
2. ID-based internal API works: `_compute_features_by_id(2025, 1181, 1276, stats_lookup)` returns same dict
3. Error handling: unknown team name raises ValueError
4. No regressions: `build_matchup_dataset()` still builds 1054+ matchups
5. No import errors in any module that uses features
</verification>

<success_criteria>
- `compute_features(team_a="Duke", team_b="Michigan", season=2025)` returns a named feature vector with all 6 FEATURE_COLS
- All existing call sites (backtest.py, bracket_schema.py, features.py internal) work without errors
- pytest and statsmodels are installed and importable
</success_criteria>

<output>
After completion, create `.planning/phases/08-feature-store/08-01-SUMMARY.md`
</output>
