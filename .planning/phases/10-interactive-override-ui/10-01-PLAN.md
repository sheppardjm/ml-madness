---
phase: 10-interactive-override-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/data_loader.py
  - app.py
autonomous: true

must_haves:
  truths:
    - "run_deterministic() and run_monte_carlo() accept override_map parameter and pass it to simulate_bracket()"
    - "st.session_state['override_map'] is initialized as empty dict on first load"
    - "Simulation calls in app.py pass override_map from session_state to cached functions"
    - "Empty override_map is normalized to None before passing to simulator"
  artifacts:
    - path: "src/ui/data_loader.py"
      provides: "Override-aware cached simulation functions"
      contains: "override_map"
    - path: "app.py"
      provides: "Session state init and override-aware simulation calls"
      contains: "override_map"
  key_links:
    - from: "app.py"
      to: "src/ui/data_loader.py"
      via: "override_map parameter in run_deterministic/run_monte_carlo calls"
      pattern: "run_deterministic.*override_map"
    - from: "src/ui/data_loader.py"
      to: "src/simulator/simulate.py"
      via: "override_map forwarded to simulate_bracket()"
      pattern: "simulate_bracket.*override_map"
---

<objective>
Wire override_map through the simulation pipeline so that user overrides (set via session_state) flow from app.py through cached data_loader functions into simulate_bracket().

Purpose: This is the plumbing that enables all override functionality. Without override_map flowing through the cache layer, no UI controls can affect simulation results.

Output: Modified data_loader.py with override_map params on both simulation functions; modified app.py with session_state init and override-aware simulation calls.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-interactive-override-ui/10-RESEARCH.md
@src/ui/data_loader.py
@app.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add override_map parameter to cached simulation functions</name>
  <files>src/ui/data_loader.py</files>
  <action>
Modify run_deterministic() and run_monte_carlo() in data_loader.py:

1. Add `override_map: dict | None = None` parameter to both functions (NOT underscore-prefixed -- it must be hashable for cache invalidation).

2. Add `hash_funcs={dict: lambda d: str(sorted(d.items()))}` to the @st.cache_data decorator on BOTH functions. This is required because override_map is a dict that Streamlit cannot hash by default.

3. Inside each function body, normalize empty dict to None before passing to simulate_bracket():
   `override_map=override_map or None`
   This avoids unnecessary validation overhead in the simulator when no overrides exist (per research pitfall 4).

4. Pass override_map to the simulate_bracket() call in each function.

5. Update docstrings to document the new override_map parameter and the hash_funcs cache invalidation behavior.

IMPORTANT: Do NOT change the underscore-prefix convention for _predict_fn and _seedings -- those are unhashable and must remain prefixed. override_map is a plain dict and MUST be a regular (non-underscore) parameter so Streamlit includes it in the cache key.

Reference existing signatures:
- run_deterministic(_predict_fn, _seedings: dict, season: int = 2025) -> dict
- run_monte_carlo(_predict_fn, _seedings: dict, season: int = 2025, n_runs: int = 10000, seed: int = 42) -> dict
  </action>
  <verify>
Run: `.venv/bin/python -c "from src.ui.data_loader import run_deterministic, run_monte_carlo; import inspect; sig_d = inspect.signature(run_deterministic); sig_m = inspect.signature(run_monte_carlo); assert 'override_map' in sig_d.parameters, 'missing from run_deterministic'; assert 'override_map' in sig_m.parameters, 'missing from run_monte_carlo'; print('PASS: override_map parameter present in both functions')"`
  </verify>
  <done>Both run_deterministic() and run_monte_carlo() accept override_map parameter with hash_funcs cache invalidation, forwarding to simulate_bracket()</done>
</task>

<task type="auto">
  <name>Task 2: Add session_state initialization and override-aware simulation calls in app.py</name>
  <files>app.py</files>
  <action>
Modify app.py to initialize override state and pass it through simulation calls:

1. Add override_map session_state initialization right after the existing season init block (around line 27):
   ```python
   if "override_map" not in st.session_state:
       st.session_state["override_map"] = {}
   ```

2. After the session_state init, extract the override_map and normalize it:
   ```python
   override_map = st.session_state.get("override_map", {})
   _override_arg = override_map if override_map else None
   ```

3. Update the simulation calls (around line 38-39) to pass override_map:
   ```python
   det_result = run_deterministic(predict_fn, seedings, season, override_map=_override_arg)
   mc_result = run_monte_carlo(predict_fn, seedings, season, override_map=_override_arg)
   ```

4. Add an override count display in the sidebar, after the MC champion section (around line 57):
   ```python
   if override_map:
       st.divider()
       st.warning(f"{len(override_map)} manual override(s) active")
   ```

Do NOT change any other parts of app.py yet. The override controls UI, reset button, and SVG visual feedback are handled in Plan 02.
  </action>
  <verify>
Run: `grep -n "override_map" /Users/Sheppardjm/Repos/madness2026/app.py` should show:
- Session state initialization line
- override_map extraction line
- _override_arg normalization line
- run_deterministic call with override_map
- run_monte_carlo call with override_map
- Sidebar override count display

Run: `.venv/bin/python -c "import ast; tree = ast.parse(open('app.py').read()); print('PASS: app.py parses without syntax errors')"` from project root.
  </verify>
  <done>app.py initializes override_map in session_state, passes it to both simulation functions, and shows override count in sidebar</done>
</task>

</tasks>

<verification>
1. `cd /Users/Sheppardjm/Repos/madness2026 && .venv/bin/python -c "from src.ui.data_loader import run_deterministic, run_monte_carlo; print('Import OK')"` -- imports without error
2. `cd /Users/Sheppardjm/Repos/madness2026 && .venv/bin/python -c "import ast; ast.parse(open('app.py').read()); print('Syntax OK')"` -- no syntax errors
3. `grep -c "override_map" src/ui/data_loader.py` -- should be >= 8 (2 signatures + 2 docstrings + 2 normalize lines + 2 simulate_bracket calls)
4. `grep -c "override_map" app.py` -- should be >= 5 (init + extract + normalize + 2 calls + sidebar)
</verification>

<success_criteria>
- override_map flows from app.py session_state through data_loader cached functions to simulate_bracket()
- Cache invalidation via hash_funcs ensures different override_maps produce different cached results
- Empty override_map normalized to None to skip unnecessary simulator validation
- No regressions: app still works identically when override_map is empty (default state)
</success_criteria>

<output>
After completion, create `.planning/phases/10-interactive-override-ui/10-01-SUMMARY.md`
</output>
